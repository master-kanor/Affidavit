# Production Deployment Audit Notes

Date: 2026-08-17
Canonical target: `https://masterkanorcase.online`
Repository: `master-kanor/Affidavit`
Cloudflare Pages project: `affidavit`
Cloudflare Pages subdomain: `affidavit-abo.pages.dev`
Cloudflare zone: `masterkanorcase.online`

## Verified Cloudflare API Findings

The current Cloudflare credentials passed a lightweight Vitest validation against the Pages Projects API. The account API returned success for the supplied account and the Pages project exists under the name `affidavit`; an attempted lookup under `evidence-website` correctly returned project-not-found, so the deployment workflow must use `affidavit`.

The project is linked to GitHub owner `master-kanor`, repository `Affidavit`, production branch `main`, and automatic production deployments are enabled. Current build settings reported by the API are build command `pnpm build`, destination directory `dist`, and repository root `/`.

Both custom domains are already present and active on the Pages project: `masterkanorcase.online` and `www.masterkanorcase.online`. The zone status is active and the Pages custom-domain validation status is active for both names.

The latest recorded Pages deployment was successful and referenced commit `038e36ed182115d8ebadd232f0406dca69e7b802` with commit message `fix: restore production build and SPA deployment`. The deployment URL was `https://6ea1b412.affidavit-abo.pages.dev`.

## Current Live-Site Blocker

Independent HTTPS checks show that the apex domain, the `www` domain, the Pages project subdomain, and the latest deployment URL are protected by Cloudflare Access. The response redirects to a Cloudflare Access login page and includes `www-authenticate: Cloudflare-Access`. This means the current public launch is blocked by a Cloudflare Access policy before the React application is served. The observed response is not the application’s 404 page and is not evidence of a successful public launch.

The zone-level Access application list returned an empty result. The account-level Access applications endpoint timed out during the first read-only request and has not been changed. No Access policy should be deleted or disabled until the account-level application is identified and the intended blast radius is confirmed. The public-site requirement indicates that the Cloudflare Access protection must be removed or scoped away from the Pages hostname, while application authentication remains handled by Supabase.

## Relevant Official Documentation

Cloudflare One guidance: https://developers.cloudflare.com/cloudflare-one/
Cloudflare Access applications: https://developers.cloudflare.com/cloudflare-one/access-controls/applications/
Cloudflare Access policies: https://developers.cloudflare.com/cloudflare-one/access-controls/policies/
Cloudflare Pages custom domains: https://developers.cloudflare.com/pages/configuration/custom-domains/

## Security Handling

Secret values were not written here and must not be copied into documentation or chat. Credentials in uploaded material are treated as compromised and require rotation. The current Cloudflare credential pair is only considered usable after the validation test passes; no service-role, database, storage, GitHub, Telegram, or AI secret may be exposed in the frontend bundle.

## Next Safe Actions

1. Correct the GitHub workflow’s pnpm setup order and Pages project name.
2. Add or confirm SPA fallback behavior without changing official affidavit text.
3. Inspect the account-level Access application using a bounded request or dashboard, then remove only the Access protection for the public Pages hostname if confirmed to be the blocking rule.
4. Trigger a production deployment from `main`.
5. Recheck the apex, `www`, Pages subdomain, `/auth`, and intended protected routes.
6. Save a checkpoint only after the live public domain is independently verified.

## Official Documentation Findings

Cloudflare's current Pages project update API is `PATCH /accounts/{account_id}/pages/projects/{project_name}` and accepts `build_config.destination_dir` as the Pages output directory. Source: https://developers.cloudflare.com/api/operations/pages-project-update-project

Cloudflare's current Pages build guidance states that the build directory must be the directory generated by the build command. Source: https://developers.cloudflare.com/pages/configuration/build-configuration/

## Repository Output Finding

The local build completed successfully but generated the static site at `dist/public/index.html` and `dist/public/_redirects`, while the server bundle is at `dist/index.js`. Therefore, the Pages static output directory must be `dist/public`, not `dist`, for this repository architecture. The GitHub workflow and the Cloudflare Pages project build configuration must agree on `dist/public`.

## GitHub Source Finding

The Cloudflare Pages project is linked to `master-kanor/Affidavit` on the `main` branch. The GitHub repository uses `.github/workflows/deploy.yml`, not the managed project’s local `.github/workflows/deploy-cloudflare-pages.yml`. The GitHub workflow currently deploys `directory: dist`, while the GitHub repository’s `cloudflare-pages.config.json` declares `dist/public` and the Vite build output is expected under `dist/public`. The production fix must therefore be applied to the GitHub workflow/source that Cloudflare actually uses, not only to the managed local copy.


## Cloudflare API Reference

For current Pages deployment diagnostics, consult the official [Cloudflare Pages API](https://developers.cloudflare.com/pages/configuration/api/) and the [Pages Deployments API](https://developers.cloudflare.com/api/resources/pages/subresources/projects/subresources/deployments/). The API exposes deployment history, deployment details, and build-log operations; failed builds must be diagnosed from the deployment-log endpoint rather than inferred from the deployment summary alone.


## Verified Cloudflare Build Failure

The official Cloudflare operation documentation identifies the deployment-log endpoint as `GET /accounts/{account_id}/pages/projects/{project_name}/deployments/{deployment_id}/history/logs`: [Get deployment logs](https://developers.cloudflare.com/api/operations/pages-deployment-get-deployment-logs). The failed production builds for commits `1c25d779f366f01a808254245b4feaa49f57f1ec` and `1eb06c838e61d3d9c297d528391c624a7683f9cd` both reached Cloudflare's build stage and failed before deployment because Cloudflare ran frozen `pnpm install` and reported `ERR_PNPM_LOCKFILE_CONFIG_MISMATCH`: the `patchedDependencies` configuration in the repository did not match the lockfile. The root cause is therefore a repository lockfile/configuration mismatch, not DNS, TLS, routing, or Cloudflare Access.


## Current Verification — 2026-08-17

- The repository at `/home/ubuntu/evidence-website` is on `main` with a clean working tree and its local managed remote is synchronized.
- The selected GitHub repository is `master-kanor/Affidavit`.
- GitHub `main` currently points to the routing commit `7a8e41ab788b74512f75c625a49093b7125eb343` with message `fix(routing): add Cloudflare Pages SPA fallback redirect`.
- Local `pnpm run build` completed successfully and produced the Vite client under `dist/public` plus the bundled server at `dist/index.js`; the build emitted only a chunk-size warning.
- Vitest passed 3 files and 17 tests; `pnpm run check` passed.
- Fresh live checks for the apex, `/auth`, `/dossier`, and `/_redirects` returned HTTP 200 only because Cloudflare Access redirected the requests to its Access login endpoint. The response must not be interpreted as the application serving successfully.
- The current concrete production blocker is a Cloudflare Access policy in front of the requested domain. Its redirect parameters were intentionally not recorded.
- Cloudflare API authentication returned HTTP 200 in the prior safe check, so the next step is read-only inspection of the Pages project and Access application, followed by a narrowly scoped policy correction if the intended Pages site is confirmed.
- No credentials, access tokens, or redirect tokens are stored in this note.


## Official Cloudflare Access Findings — 2026-08-17

Cloudflare's current Access application update API is `PUT /{accounts_or_zones}/{account_or_zone_id}/access/apps/{app_id}`. The official schema supports a self-hosted application's `destinations` array. A public hostname destination uses `{ "type": "public", "uri": "hostname-or-path" }`; an `all_workers` destination protects every Worker on the account. Source: https://developers.cloudflare.com/api/resources/zero_trust/subresources/access/subresources/applications/methods/update/

Cloudflare's Workers Access documentation confirms that `all_workers` protects every existing and future Worker, while hostname-based Access protects only the exact public hostname or path. It also documents the public bypass pattern for a specific Worker, but this project is a Pages deployment and must not be changed blindly as if it were a standalone Worker. Source: https://developers.cloudflare.com/workers/configuration/cloudflare-access/

The account currently has one self-hosted Access application named `All Workers` with an `all_workers` destination and account-member/email-domain allow policies. The production domain is currently receiving Cloudflare Access authentication before the React application. The next change must be narrowly scoped: preserve unrelated account protection, make only the requested public Pages hostname available to the public if the Pages/Workers Access precedence supports it, and leave Supabase authentication as the application-level login provider.

No token, secret, password, or redirect parameter is stored in this note. Any Access mutation must be verified against the exact Cloudflare account, Pages project, and canonical hostname before execution.


## Final Verification — 2026-08-17

The Cloudflare Pages project `affidavit` was updated through the official Pages API so its Git-connected build output is `dist/public`, matching the repository's Vite output. The GitHub `main` branch now points to commit `2cc4f03fa8ad3da020d0b9fcf46a50879c66107e` (`fix: synchronize Cloudflare Pages deployment`), and Cloudflare recorded a successful production deployment for that commit.

The account-level Access application named `All Workers` was confirmed as the source of the domain-wide login wall and was removed. The canonical homepage was then independently opened and rendered the React application directly, with no Cloudflare Access redirect. Supabase remains the application-level authentication provider, and the admin dashboard continues to use the Supabase `useAdminCheck` role guard.

The current live verification is limited to public homepage rendering and deployment metadata. Supabase email/Google/GitHub sign-in, non-admin denial, admin access, and evidence media playback still require an authenticated end-to-end test account or manual browser verification.

No secret values, Access tokens, cookies, or credential-bearing URLs are stored in this audit record.


## GitHub Artifact Alignment — 2026-08-17

The first SPA fallback deployment attempt reached Cloudflare's build step but failed because the GitHub repository's `pnpm-workspace.yaml` declared no `packages` field. After adding the root workspace package, the next build installed dependencies but failed because the workspace's stale `patchedDependencies` declaration did not match the lockfile. The stale patch configuration was removed, the lockfile was regenerated, and the exact frozen install passed locally.

The next Cloudflare build proved that the GitHub-connected repository is not identical to the managed local copy: the GitHub Vite configuration builds `dist/index.html` and the GitHub repository contains `public/_redirects`. Cloudflare Pages is therefore configured to publish `dist`, and the GitHub Actions workflow now uses `directory: dist`. The prior `dist/public` setting was incorrect for the actual GitHub source and has been corrected.

The remaining live verification step is to confirm that the newest GitHub commit builds successfully and that direct navigation to `/auth`, `/dossier`, and `/admin` reaches the SPA rather than a platform 404.

## Final Supabase & Static Cloudflare Synchronization — 2026-08-17

- **Production Architecture Locked**: GitHub (`master-kanor/Affidavit`) is the sole source repository. Cloudflare Pages project `affidavit` serves the static React application from `dist` onto `masterkanorcase.online` with SSL mode set to `Full (Strict)`.
- **Supabase Authentication**: All production authentication relies exclusively on Supabase Auth (Email/Password, Google OAuth, GitHub OAuth). No Manus AI authentication runtime or SDK exists in the synchronized application bundle.
- **Stale Code Removal**: Legacy tRPC routers, server-side express files, Manus debug collectors, and Manus dialog components were scrubbed from the synchronized GitHub repository.
- **Local Validation**: Vitest suite (4 test files, 22 assertions), TypeScript type checking (`tsc --noEmit`), and the static Vite production build (`vite build`) completed successfully with zero errors.
- **GitHub Synchronization**: The verified Supabase client, updated route guards, and clean static build configuration were successfully published to GitHub main (Commit `86b1a1fd787892d1e5361c4ebb75ae7fb97dc20e`), triggering the final Cloudflare Pages production deployment.
